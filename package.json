{
  "name": "vonga-website",
  "version": "1.0.0",
  "description": "Vonga marketing website with blog",
  "scripts": {
    "build:blog": "node scripts/build-blog.js",
    "dev": "npx http-server -p 3000 -o",
    "prebuild": "npm run build:blog"
  },
  "dependencies": {},
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^14.1.3",
    "date-fns": "^4.1.0"
  }
}
